package ${targetPackage};

import javax.ws.rs.core.Response;

import ${resourceRepresentation.qualifiedName};

interface I${resourceName}ResourceClass {
	Response create(${resourceRepresentation.name} rro);
	Response getByBusinessKey(String businessKey);
	Response removeByBusinessKey(String businessKey);
	Response update(String businessKey, ${resourceRepresentation.name} rro);
}